function w = bepositive(v)
w=[];
w = v(v>=0);
%widx=1;
 %for vidx = 1:length(v)
    %if v(vidx)>=0  %pick only the positive values from the input vector ...
        % w(widx) = v(vidx); % original way 
        % widx = widx + 1; % original way 
        % w = [w v(vidx)]; % appending the v(vidx) to the w 
       
   % end
end